-- optional problem-statement use case: alert on concurrency decline
-- rule: minute-over-minute drop >= 50%, base concurrency >= 50
-- deterministic threshold on marts.v_occupancy_minute, not an LLM call
-- (see module docstring for why); query_id 6b2a2c39-54f0-43a7-9b88-a27b7ebd3879

minute 29751090: 214 -> 7 sessions (96.7% drop). possible causes per the problem statement: asset ended, system issue, or content not engaging; the alert flags the minute, a human or a downstream LLM call decides which.

narration (openai.gpt-oss-120b via Bedrock, one call, optional, off by default): The abrupt 96 % dip from 214 to just 7 viewers in one minute looks like the stream finished rather than viewers slowly losing interest, and it’s an isolated collapse (a system glitch would usually affect several assets at once). So the most likely cause is the **asset ending**.
